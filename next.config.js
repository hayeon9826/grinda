/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["tailwindui.com", "images.unsplash.com", "img.freepik.com"],
  },
};

module.exports = nextConfig;
